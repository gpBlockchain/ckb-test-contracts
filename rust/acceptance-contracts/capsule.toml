# [rust]
# # path of rust contracts workspace directory,
# # a `Cargo.toml` file is expected under the directory.
# workspace_dir = "."

# capsule version
version = "0.10.1 25fd7d4"
# path of deployment config file
deployment = "deployment.toml"

[[contracts]]
name = "spawn_demo"
template_type = "Rust"

[[contracts]]
name = "always_success"
template_type = "Rust"

[[contracts]]
name = "loop_contract"
template_type = "Rust"

[[contracts]]
name = "ckb_get_memory_limit"
template_type = "Rust"

[[contracts]]
name = "set_content"
template_type = "Rust"